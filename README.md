# open-exchange-php-function
convert to USD

just write the next

```
$valueInUSD = convert_currency_USD("EUR", 129);
```
Where the first parameter is currency code and the second - value in this currency
